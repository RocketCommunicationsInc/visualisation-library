import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { data } from '../stencil-asset/data/data.json';
import { html, render } from 'lit-html';

<Meta title="Graph" component="my-graph" />

# My Graph

Example Doc

export const Graph = args => {
  return html`<div id="graph" style="padding: 5%; display: flex; justify-content: center;">
    <my-graph .points="${args.points}"></my-graph>
  </div>`;
};

<Canvas>
  <Story
    name="Graph"
    args={{
      points: data
    }}
  >
    {Graph.bind()}
  </Story>
</Canvas>
