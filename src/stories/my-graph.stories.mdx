import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { data } from '../stencil-asset/data/data.json';
import { html, render } from 'lit-html';

<Meta title="Components/My Graph" component="my-graph" />

# My Graph

Example Doc

export const Default = args => {
  return html`<div style="padding: 5%; display: flex; justify-content: center;">
    <my-graph .points="${args.points}" .random="${args.name}"></my-graph>
  </div>`;
};

<Canvas>
  <Story
    name="Default"
    args={{
      points: data,
      random: 'Hello',
    }}
  >
    {Default.bind()}
  </Story>
</Canvas>
