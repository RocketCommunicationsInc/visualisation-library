import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { html, render } from 'lit-html';

<Meta title="Pie" component="my-pie-chart" />

# My Pie

Example Doc

export const Pie = args => {
  return html`<div id="pie" style="padding: 1%; display: flex; justify-content: center; max-width:300px; max-height:300px;">
    <my-pie-chart
      .data=${args.data}
      .radius=${args.radius}
      .viewBox=${args.viewBox}
      .borderSize=${args.borderSize}
    ></my-pie-chart>
  </div>`;
};

<Canvas>
  <Story
    name="Pie"
    args={{
      data: [
        {
            id: 1,
            percent: 35,
            color: 'DarkSeaGreen',
            label: 'Slice 1',
        },
        {
            id: 2,
            percent: 15,
            color: 'DarkOrchid',
            label: 'Slice 2',
        },
        {
            id: 3,
            percent: 22,
            color: 'DodgerBlue',
            label: 'Slice 3',
        },
    ],
      radius: 50,
      viewBox: 100,
      borderSize: 20
    }}
  >
    {Pie.bind()}
  </Story>
</Canvas>
